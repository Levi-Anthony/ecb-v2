# ECO-163 — Integral Coherence Contract Move Commission — 2026-09-19

## CONTROL

Execute a bounded **Move / operational qualification** of the substrate-independent finite-coherence architecture frozen by ECO-162.

**Parent architecture program:** ECO-136.
**Sense:** ECO-161 — PASS WITH MATERIAL CORRECTION.
**Shape:** ECO-162 — PASS.
**Shape return:** Linear document `3bf5f910-ca37-4619-8dae-99b2e311c9f2`, slug `fecb8e1c73a8`.

**Current phase standing:** COMMISSIONED ONLY. Do not execute from the commissioning session. The Principal authorized descent to the Move border and explicitly stopped there.

## Governing Move question

> **Does the shaped tri-axial + fidelity architecture actually preserve coherent finite engagement under real variation, discontinuity, and reactivation without false coupling, relevance-first deletion, context-volume dependence, or hidden replacement machinery?**

This is an evidence-bearing qualification Move, not implementation installation.

## Frozen architecture under test

Macro axes:

1. semantic/open-world composition;
2. locally governing consequentiality;
3. temporal/coordination governance.

Cross-cutting fidelity contract:

* referential identity/grain/boundary;
* mapper/frame/access;
* evidence/warrant/standing;
* authority/custody/currentness separation;
* possibility preservation;
* typed transition/requalification;
* reconstruction fidelity;
* bounded challenge path.

Composition rule:

A current engagement may be exposed as a composed projection/view, but independently variable constituents retain their own identity, lineage, standing, and transition semantics.

Disposition grammar is orthogonal:

* coverage: examined / unexamined;
* activation: active / dormant;
* disposition/standing: relied-for-declared-use, unresolved, nonconsequential-now, conditional/sensored, rejected-with-warrant, prohibited/impossible-under-qualified-rule.

Do not replace this with one active/inactive or one generic relevance score.

## Evidence cases

Execute all four cases unless a case exposes an outcome-determinative upstream failure that requires stopping.

### Case A — reflective architecture reorientation

Use the actual ECO-160 → Metabolize → ECO-161 → ECO-162 lineage.

Test whether the architecture can represent:

* the original narrower naming referent;
* the Principal's challenge;
* deeper referent opening;
* BRIMAR remaining qualified for the earlier projection while adoption becomes held;
* FIBERR/Filament correction;
* Sense closure;
* Shape closure;

without rewriting the prior history or making the new referent retroactively identical to the old one.

This case tests reorientation, historical validity, referent/projection distinction, and phase/currentness.

### Case B — cold handoff / reconstruction

From the shaped contract, create a bounded cold-readable **engagement projection** for one live ECOS inquiry.

Give that projection to a fresh worker/context that does not receive conversational memory or unrestricted history.

Require the worker to reconstruct at minimum:

* focal referent and grain;
* consequential boundary/frame;
* governing orientation;
* current coordination phase/episode;
* what is relied upon versus unresolved;
* dormant/sensored material;
* authority/currentness limits;
* exact next Move and stop rule.

Compare the reconstruction to the source-of-truth surfaces.

The handoff packet itself must not be treated as currentness or authority.

### Case C — unfamiliar-domain/open-world specimen

Select and freeze one source artifact from a domain not already encoded as an ECOS-specific schema and not chosen because it trivially fits the model.

Before analysis:

* record exact source identity/version/hash/URL or durable file reference;
* declare the inquiry/desired end state;
* prevent prior ECOS-specific object typing from becoming the answer.

Apply the shaped architecture to admit/seat the relevant referents, qualify frame/access, derive governing consequentiality, manage active versus dormant material, and advance through a bounded coordination sequence.

Test whether novel semantic structure can be represented without a new universal object schema.

### Case D — authority/currentness/action negative-space case

Use the existing ECO-151/ECO-152 orientation-lifecycle evidence as a fixed specimen; do not mutate production.

Exercise the Shape contract against:

* Resolution exists but is not current;
* Qualification PASS but not current;
* current binding with separate authority standing;
* reviewer revocation without erasing historical qualification;
* binding-authority revocation distinct from semantic qualification;
* withdrawal/no-current;
* material/unknown change requiring requalification;
* historical evidence remaining recoverable.

Confirm that the broader architecture preserves the narrower qualified separations and does not let phase, relevance, or representation create authority.

## Required coordinate perturbations

Across the cases, deliberately perturb at least these conditions one at a time. Record expected versus observed affected surfaces.

1. same referent, different frame/access;
2. same referent/frame, different governing orientation;
3. same referent/G, changed evidence/warrant;
4. same semantic basis, changed binding authority/currentness;
5. same active engagement, QF/sensor on dormant material fires;
6. changed focal grain or boundary;
7. changed SSMM phase/coordination state without new authority;
8. worker/session handoff;
9. grammar/version change whose materiality is unknown.

A test FAILS if a supposedly independent change silently mutates another coordinate without an explicit qualified transition.

## Dormancy/reactivation proof

Demonstrate at least one material item that is:

1. examined;
2. presently nonconsequential or conditional;
3. dormant rather than deleted;
4. paired with a calibrated QF/sensor;
5. later reactivated by the declared signal;
6. reintroduced without retroactively claiming it had been active/current all along.

This is mandatory evidence for **prune computation, not possibility**.

## Reconstruction fidelity test

The fresh worker must not merely repeat packet text.

Ask it to perform at least one new downstream classification or next-step judgment whose correctness depends on reconstructing the engagement.

Evaluate:

* focal continuity;
* frame attribution;
* governing-orientation fidelity;
* coordination/currentness;
* disposition of unknown/dormant material;
* authority limits;
* next-step/stop correctness.

A verbose context dump that produces semantic promotion or wrong-frame reasoning fails even if it contains more source text.

## Compatibility test against current orientation lifecycle

Explicitly map the shaped architecture onto the existing qualified BUILD-7′ distinctions:

* Orientation Scope;
* Orientation Resolution;
* Qualification;
* Authority Decision;
* Binding Transition;
* current binding/epoch;
* material-change fencing;
* requalification;
* withdrawal/no-current;
* cold recovery.

Report whether the new architecture:

* preserves them;
* clarifies them;
* exposes an actual conflict;
* or merely redescribes them.

Do not reopen ECO-151/ECO-152 on analogy alone.

## Anti-confirmation controls

At least one worker/test lane must receive the cases without being told that “three axes” is the expected answer. Ask it to classify required concerns and transition failures independently.

Preserve contradictory output rather than averaging it away.

Do not treat successful use of the vocabulary as proof of the architecture.

## Acceptance surface

Move passes only if:

 1. all three macro axes demonstrate distinct decision-relevant work across cases;
 2. no tested axis can be removed without hidden replacement of its characteristic work;
 3. cross-cutting coordinates can vary independently where expected;
 4. coverage, activation, and disposition remain distinct;
 5. dormant material can be reactivated through its declared QF/sensor;
 6. fresh-worker reconstruction preserves governing shape without context-volume dependence;
 7. current orientation lifecycle separations survive unchanged unless evidence demonstrates a real conflict;
 8. historical instruments remain noncollapsed;
 9. at least one generative prediction from ECO-161 is observed or falsified;
10. failures are sufficient to identify whether repair belongs in Sense, Shape, the specimen, or later physical design.

No weighted score. A material false-coupling or possibility-deletion failure is not compensated by other passes.

## Required return

Produce a cold-readable Move return containing:

 1. currentness/source receipts;
 2. frozen test protocol and exact specimens;
 3. independent-control worker instructions and outputs;
 4. four case results;
 5. coordinate-perturbation matrix;
 6. dormancy/reactivation trace;
 7. cold-handoff reconstruction and gold comparison;
 8. BUILD-7′ compatibility map;
 9. observed generative predictions;
10. failure/repair ledger;
11. FACT / OBSERVATION / INFERENCE / UNKNOWN register;
12. Register-B qualification verdict;
13. exact architecture repairs if needed;
14. naming impact only if evidence changes the referent;
15. Metabolize seed / exact next seam.

## Hard boundaries

Do not:

* install or mutate canonical Supabase/runtime;
* select physical database/API schema;
* deploy a coherence service;
* adopt BRIMAR, BRAID, or another proper name;
* rewrite historical Decoder Ring, Master Key, FIBERR, Filament, or SSMM artifacts;
* treat legacy Open Brain as canonical ecb-v2-brain;
* optimize for minimum/smallest;
* infer authority from phase, relevance, packet possession, or model output.

Repository/Linear documentation publication is allowed as evidence continuity. Any branch preview caused automatically by Git integration is publication-side effect only, not mechanism installation.

## STOP

Stop after the Move return and publication/receipts.

Do not automatically enter Metabolize or physical design. Return the decision surface to the Principal.