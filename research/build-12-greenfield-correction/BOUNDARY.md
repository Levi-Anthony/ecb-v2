# Corrective boundary

The following first-pass BUILD 12 semantics are not accepted merely because they exist physically: artifact keys, artifact type taxonomy, numbered version families, native supersession links, generic media/provenance/created-by fields, and implicit latest-version selection.

The corrective build may preserve implementation history but must not depend on these semantics.
