# Bounded follow-up, declared before the second deployment/calls

The four initial observations are retained without replacement. They show participant omission,
generic relevance questions, and one altered source excerpt. The original system message also says
no continuation was supplied, which is an avoidable mismatch for the branch cases.

Change only the instruction profile: explicitly separate participants from situations, prefer
identity/purpose/currentness discriminators, require unchanged excerpt punctuation, and say only
that no external database evidence is supplied. Keep the model, schema, source text and checking
code fixed. This is extraction prompt iteration, not a tokenizer output design or new ontology.

Prediction: base and gift cases will disclose participants separately, the gift case will satisfy
the excerpt checker, and questions will be more locally useful. Failure: any of those does not
occur. Preserve overinterpretation even if structure passes. Stop after these two calls; no claim
of held-out generalization, repeatability, automatic identity resolution or complete Integral grammar.
