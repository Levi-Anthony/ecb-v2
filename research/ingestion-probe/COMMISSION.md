STATUS: PRINCIPAL-AUTHORIZED BOUNDED EXPERIMENT
DISPOSITION: COMMISSION RECORD / NOT A NEW ONTOLOGY
DATE: 2026-09-15 UTC
BASE: 6aec8b97f41cb0c228f6cbf2e99718f43b0d1882

# Register B recalibration and ingestion preparation probe

The Principal explicitly accepted the Register B disposition in the session titled
REGISTER B CYBERNETIC RECALIBRATION + INTEGRAL TOKENIZATION FIRST PHYSICAL PROBE.
After alignment corrections, the Principal said: “Aligned. Go”.
This is an assistant-recorded account of that authority, not independent authority.

The original commission authorizes minimum governing-document edits, bounded experimental code,
fixtures/tests, routine Git operations, and installed v2 experimental interaction where existing
authority permits. Subsequent corrections narrow the immediate experiment to raw atomic Thought
preparation and differentiation for database encounter. Full tokenizer output, downstream worker
navigation, reminders, scheduled review, and action execution are not this experiment.

## Principal intent and inherited constraints

- OB1 is the kernel; legacy ECB supplies evidence. Reuse existing OpenRouter capability and code
  before proposing additional vendors. No vendor or runtime migration is authorized here.
- Universal Referents are stable addressable subjects, including situations, relations, and
  unresolved subjects of inquiry, not merely named people or nouns.
- Required Integral structural capacities remain inherited grammar: quadrantic quality, relational
  level, Lines, Stages, States, Types, paired directional Drives/Capacities, and independent
  focal/frame/mapper/access distinctions. This probe does not recreate missing finer doctrine.
- Master Key governs activation/resolution, not existence or truth. Levi is the parent focal
  referent; self-improvement is its telos. The local task is testing ingestion preparation, not
  making autobiographical claims about Levi.
- “Jennifer wanted me to call her back” and both continuations are Principal-supplied illustrative
  fixtures, not verified events. Do not ingest them as actual personal history or create reminders.
- Tokenization is a new mechanism hypothesis serving older dynamic referent discovery, activation,
  and composition. An experimental annotation format is not a final Integral Token interface.

## Initial bounded unit (subsequently revised by the tool commission below)

PURPOSE: test whether input differentiation can preserve a reported situation and unresolved purpose
for later database encounter without premature task classification or entity merging.
INVARIANT SERVED: identity != description; evidence != assertion; shared storage != shared semantics;
qualification != truth/authority; local resolution != inherited architecture.
INPUTS: the exact base sentence, two isolated continuation fixtures, current governing sources,
live catalog and source reads, and an explicitly hand-authored candidate annotation.
OUTPUT: reproducible preparation/checking code, rollback-only substrate observations, exact limits,
and a credential-gated OpenRouter extraction experiment ready for execution.
STANDING: proposed experimental representation; semantic performance unestablished until measured.
ENFORCEMENT: code validates correspondence/shape; installed SQL constrains actual persistence;
semantic adequacy remains separately evaluated. No model output can write to production.
FAILURE BEHAVIOR: invalid annotation rejected locally; SQL effects always rolled back, including
on unexpected failure. No provider/database capability changes.
APERTURE: exact automatic extraction quality; later identity resolution; physical carriage of the
complete inherited grammar; readiness of consumer routing.
REVALIDATION TRIGGER: source drift, contrary input, failed discrimination, loss of source/identity,
or a proposed persistent representation exceeding installed scope.
TEST: negative controls plus a real database subtransaction; compare both continuations without
showing either continuation to the base-sentence extractor.
NON-GOAL: final ontology/token schema, identity merger, new MCP tools, new production schema, runtime
cutover, full Universal Referent Walk, or constitutive proof of Integral Tokenization.

### Subsequent Principal correction — tool commissioning

The Principal explicitly instructed: “Commission and make the tool so it can continue to produce
evidence.” This supersedes the earlier new-tool exclusion above for the required ingestion instrument.
It also ends the proposed local-database detour. A missing capability is evidence for a bounded tool,
not a reason for repeated workaround construction. Access restrictions remain binding.

Commissioned tool: `disclose_atomic_thought`, an authenticated HTTP experimental function on the
existing legacy `open-brain` project, using its already-installed OpenRouter secret. This is a
separate instrument, not a change to legacy capture, an ECB database migration, or a replacement
ordinary runtime. Its source lives in `server/ingestion-experiment/`. It imports no database client,
reads no database credential, and has no database/action effect. Shared hosting still supplies the
legacy environment; code-level non-use is not claimed to be provider-level capability confinement.

Experimental invocation uses a locally generated secret whose hash, not value, is deployed, with a
one-hour expiry. Each invocation accepts at most 8,000 text characters, fixes the model and grammar,
permits one provider request with at most 2,200 output tokens, and has a 45-second provider timeout.
No retry/model fallback is automatic. The expiry bounds the commissioned access window, not a global
request-count or spend guarantee. The run uses four predeclared illustrative input cases and one
authentication negative control. Continued availability after expiry needs explicit recommissioning
of access under the applicable commission; this may proceed autonomously when the same authority
and qualification floor holds. The reusable tool source remains preserved. The observed first-run
failures commissioned two additional calls, predeclared in `evidence/FOLLOWUP.md`.

## Predeclared predictions and stop

1. Actual Referent registration will support stable IDs for people, the reported desire, the
   unresolved occasion/contrast, and the relationship, independently of native descriptions.
2. Existing Claims/Evidence Links can preserve a scoped unassessed assertion about this fixture.
3. The installed typed-relation vocabulary will reject `wanted_callback_from`; do not substitute
   `depends_on`. This does not show that the architectural general-relation capacity is absent.
4. Registration alone will not make those subjects semantically discoverable or perform co-reference.
5. A hand-authored differentiation can retain the same base situation across both continuations.
   This demonstrates representational compatibility only, not automated semantic competence.
6. The literal-word/source-span checker will catch corrupted grounding but cannot prove semantic
   truth or distinguish all overinterpretations. Include a grounded-but-wrong negative example.

Stop the first probe after observing these boundaries, verifying rollback, and identifying the next
specific missing evidence. Do not build downstream behavior to compensate for an untested extractor.

## Recalibration receipt

The Build Contract is the single governing home for the new disposition. START_HERE adds only a
point-of-entry link. Existing structural-leverage wording already permits enforcement without
repeated incidents and is preserved. Existing frozen tests/invariants and historical phase records
are unchanged. The new text supersedes an interpretation that uncertainty necessarily requires
pre-action conceptual closure; it does not assert that every previous worker/source made that error.
Cold workers can qualify action against the six explicit floor conditions and preserve nonblocking
unknowns rather than reopening the entire Shape. Actual observations and cold reentry are in
[`RESULT.md`](RESULT.md); the initial SQL write prediction was not executable through the read-only
connector and must not be reported as a successful rollback experiment.
