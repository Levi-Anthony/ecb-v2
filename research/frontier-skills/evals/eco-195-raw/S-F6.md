SELECT ecos-frontier-exploration — The task supplies an earned, stipulated evidence floor and asks for potentially useful structural connections and discriminators. No single formal correspondence is selected for proof; route approval is expressly outside scope.

## Answer

**Evidence floor and boundary.** The service has verified exact survival of stop identifiers and arrival timestamps across a map projection change. Wheelchair access for the new route planner has not been established and requires a separate on-site assessment. These are the only facts used here. The following connections are **speculative inquiry tools**, not findings about the route or grounds for approval.

| Candidate structural connection | Why it might help the decision | Imported assumption and earliest mismatch | Discriminator and next branch |
|---|---|---|---|
| **Invariant under a change of representation.** A projection can change coordinates while selected record fields remain identical. Treat identifiers and timestamps as the specifically verified invariants, and ask which other claims would need separate preservation evidence. | It supports continued use of those two fields in record matching and time comparisons, within the verified scope. It prevents the projection receipt from silently becoming a receipt for planner usability. | Assumes the verification covers the records and transformation relevant to the planner's intended use. A mismatch arises if route selection, stop geometry, or data provenance differs from what was checked. This connection says nothing about physical access. | Trace a representative planner output back to the verified transformed records: do its stop IDs and times fall within the checked scope? If yes, retain that narrow transport result and move to the access assessment; if no, repair or extend the record check before relying on it. |
| **An added obligation is an added evidence slot.** Imagine two routes with identical stop IDs and arrival times, one passing and one failing an on-site wheelchair-access assessment. The verified fields cannot distinguish them. This is a logical fixture, not a claim that either route exists. | It shows why record preservation alone cannot decide access. The decision can be split into a record-transport finding and an access finding, with approval held until the latter is established for the proposed route. | Assumes the proposed planner's access obligation concerns conditions the two preserved fields do not encode. If a complete, independently qualified bridge from these records to on-site access exists, test its premises; none is supplied by the stipulated floor. | Can the on-site assessment produce different access outcomes for routes with the same preserved fields? If yes, transport is insufficient for access. If no under a specified and validated bridge, examine that bridge and its coverage rather than inferring approval from the projection check. |
| **Composition of evidence across a workflow.** If the planner consumes transformed records, an exact upstream field check can travel forward only when the planner uses the checked records in the checked way. A downstream route decision introduces its own conditions. | It identifies where to place the next check: at the link from transformed records to planner output, then at the separate physical access assessment. | Assumes a traceable record lineage from transformation to planner output. It first fails if the planner substitutes, joins, or alters records without a checked lineage; even perfect lineage would not establish access. | Pick one proposed route and trace its identifiers and times through the planner. A complete trace supports only those transported fields; a broken trace reopens the transport claim for that output. In either branch, obtain the on-site access result before any route approval decision. |

**Decision payoff.** Preserve the narrow verified record result, identify any unverified planner linkage, and hold route approval pending the separate on-site wheelchair-access assessment. The most decisive fixture is a pair of otherwise identical identifier/time records whose on-site access outcomes differ: it would expose precisely the missing access evidence. If such a pair cannot be found, that absence by itself proves nothing; the service would still need a qualified bridge or direct assessment before claiming access.
