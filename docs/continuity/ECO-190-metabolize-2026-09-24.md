# ECO-190 Metabolize — bounded qualification HOLD

Date: 2026-09-24
Register: B active implementation; selected external-review evidence routed to Register A.
Status: work episode closed at a durable reentry frontier. ECO-190 itself is not qualified/closed.

## Progressive-disclosure orientation

ECO-190 has substantial real-world Register-B evidence, but no final bounded qualification PASS. Register B is not globally blocked. The only blocked consumers are those whose explicit dependency contract requires ECO-190's final qualified-use guarantee.

## Harvest pass 1 — evidence and standing

Retain:
- canonical installation and real bootstrap/material-change evidence;
- seven earlier negative controls passed;
- real material change exposed a history-sensitive derivative-currentness defect;
- repaired semantics bind current use to dependency history/epochs;
- dedicated same-visible/different-history regression passed, rejecting historical 1/1 binding against restored-looking live 3/3 dependencies with EPOCH_MISMATCH;
- current-head material-change controls remain 3/4 because the old-epoch control encountered an RPC 504;
- deterministic operation id for that timed-out control: 10eeab8d-0c17-5339-bf0f-72e597e02b24;
- canonical inspection found no ordinary_operations row for that id, so there is no evidence of a hidden durable commit;
- no eco190-bounded-qualification PASS exists.

Do not promote:
- the transport timeout to a semantic failure;
- the successful regression to a universal currentness theorem;
- the installed architecture to a completed ECO-190 qualification.

## Harvest pass 2 — dependency exposure and propagation

Dependency boundary:
- BLOCKED: a downstream Move that explicitly requires “ECO-190 bounded qualification PASS” or an equivalent guarantee uniquely supplied by that PASS.
- NOT BLOCKED: Register B generally; unrelated SIGMA/ECOS work; consumers that need only the already-supported ECO-189/ECO-191 architecture or the field evidence already obtained.

Consumer rule: declare whether the consumer needs (A) existing architecture/field evidence or (B) final ECO-190 qualified-use guarantee. Never silently substitute A for B.

Register A route: preserve the history-sensitive-currentness specimen as a research prospect about false status resurrection after same-visible/different-history dependency change. Treat as hypothesis/prospect, not established general result.

Register B route: preserve the exact qualification-completion reentry as deferred implementation work, not a global blocker.

## Harvest pass 3 — residuals, reentry, and questions pre-answered

Q: Does the missing PASS invalidate the architecture?
A: No. It limits the qualification claim. It does not erase already-retained field evidence.

Q: Does this block Register B?
A: No, except explicit consumers of the final ECO-190 qualification guarantee.

Q: Is architecture redesign required before retry?
A: No evidence currently requires it. The remaining aperture is operational/tooling unless the retry contradicts retained semantics.

Q: Should the material-change sequence be replayed wholesale?
A: No. Current code can short-circuit already-applied state; preserve state and obtain the missing current-head control evidence with the minimum lawful action.

Q: Can the runtime-key/protected-consumer boundary be bypassed to finish?
A: No. That would weaken the thing being qualified.

Q: When may the borrowed host branch be restored?
A: Only after durable qualification PASS is verified.

## Exact reentry

1. Use a browser/session-capable worker against the already-bound protected Preview consumer.
2. Before mutation, verify canonical episode/head and confirm operation 10eeab8d-0c17-5339-bf0f-72e597e02b24 remains uncommitted.
3. Obtain a clean current-head old-epoch/material-change-control receipt using the minimum lawful action.
4. Run/read BRAIN-only reconstruction; require canonical_BRAIN_artifact_only and no GitHub read.
5. Invoke fail-closed qualification.
6. Verify durable eco190-bounded-qualification PASS in canonical BRAIN.
7. Only then restore build/eco-132-r4-runtime-confinement to 2234ca9a49eae03b8832ec202b597b8e6889b2da.
8. Reconcile Vercel consequence, update final continuity surfaces, and close ECO-190 if warranted.

## Closure

This work episode is closed. Reentry is narrow and explicit. No additional architecture/database investigation is currently required before the protected-consumer continuation.
