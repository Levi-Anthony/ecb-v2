STATUS: SENSE CLOSED BY HUMAN — FIRST ORDINARY SUCCESSION BOUNDARY SET
DISPOSITION: PROJECTION / QUESTION FORWARD CLOSED
DATE: 2026-09-09 America/Phoenix
AUTHORITY: Accepted BUILD 6 staged Move; verified M2 completion and Metabolize; Levi selected the minimal P1 direction

# BUILD 6 Sense — first ordinary succession

## Focal object

Determine the minimum next bounded action required to exercise one real ordinary policy succession after bootstrap without treating test use as human authorization, widening human or executor custody, or doing BUILD 6 closure work prematurely.

## Master Key

What is the smallest end-to-end episode that proves the now-operative P0/H governance can lawfully admit one exact successor, execute only that committed authorization, reconstruct the result after the effect, and leave enough evidence to decide whether BUILD 6 can close?

## Settled known state

M1 and M2 are closed PASS. The current canonical transition is `63bcd3fd-55f4-49bd-976f-c2bba50e6ab1`; bootstrap is exhausted; H/remit are operative; exact P0 is operative with SHA-256 `686148f540860aca57a43d8cdf02ee15a0f6314d14b54736e6baf6f1846a7664`.

The protected human service already supports WebAuthn login, exact policy review, accept/decline, own pending-decision withdrawal and current-state recovery. The restricted `ecb_governance_executor` remains LOGIN with bounded executor-only privileges and private local credential custody.

## Human disposition — exact P1 direction selected

Levi answered **Yes** to the Sense discriminator: the first real P1 should be the already-qualified minimal successor that changes only:

`requires_human_explanation: false` → `requires_human_explanation: true`.

This selects the exact candidate direction for Shape. It is not the P1 authorization itself. The actual authority-bearing decision must still be issued through the protected human session after the candidate and non-human execution seam are prepared and qualified.

The exact intended candidate is the P0 byte profile with only that boolean changed. The previously qualified test specimen has SHA-256 `2057597b340e7f324176c65847633b776e0d266407988dedbaec001359c39556` and 563 UTF-8 bytes. Shape must materialize and independently verify those exact bytes before presentation.

## Resolved custody question

The retained private executor record is sufficient to reconstruct the restricted executor connection without another installer URI or credential ceremony. M2 stored an applied credential record whose non-secret target identity is `executor:<project>:<pooler-host>` together with the private password. The project, executor username, database, pooler port and strict TLS path are already fixed by the released BUILD 6 implementation.

Therefore the minimum route is a repo-native local ordinary-succession helper that:

- reads the existing private record without printing or relocating the secret;
- validates owner, permissions, applied state, exact project and Supabase pooler target;
- constructs the restricted executor connection only in memory;
- consumes one public exact succession request;
- performs `recover` before any effect;
- executes only after authoritative recovery establishes `not_committed` at the expected predecessor;
- opens a fresh restricted connection and cold-recovers the same request after execution;
- emits only a secret-free bounded receipt;
- treats transport/error uncertainty as UNCONFIRMED and never as permission for a blind second effect.

No installer credential, hosted executor secret, human session, new database role or new authority path is needed.

## Resolved sequencing question

Prepare and qualify all non-human plumbing before the protected P1 decision. This keeps the actual human authorization as the last missing input and avoids creating a lawful but stranded pending grant because local custody tooling is incomplete.

The implementation may be prepared without a real decision ID by separating the fixed helper/candidate contract from the later public execution-request instance. After the human accepts P1, canonical state can be independently read, the exact committed decision identified, and a public execution request fixed without exposing any secret.

## Remaining later question

BUILD 6 closure evidence is intentionally deferred until one real ordinary succession has completed and been independently reconstructed. Do not replay M1/M2 or answer closure by projection.

## Sense closure

All closure conditions are satisfied:

- exact P1 direction selected by Levi;
- secret-safe execution/recovery responsibilities identified;
- preparation → protected human decision → public request fixation → recovery-first execution → independent reconstruction order is unambiguous;
- no remaining question changes the authority basis, candidate semantics, custody boundary or eligibility for Shape.

**CLOSE SENSE → ENTER SHAPE.**

Shape is limited to the minimum first-P1 seam: materialize exact P1 bytes, define the public execution-request contract, implement/qualify the smallest local private-custody runner, and prepare the protected review path. Shape may not accept P1 for Levi, widen executor custody, revisit M2, close BUILD 6, or open BUILD 7+.