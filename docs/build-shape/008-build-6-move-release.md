STATUS: STAGED MOVE RELEASED — CONSTRUCTION AND DISPOSABLE QUALIFICATION AUTHORIZED
DISPOSITION: DECISION_RECORD / RELEASED OUTPUT CONTRACT
DATE: 2026-09-07 America/Phoenix

# BUILD 6 — Complete authentication and activate bounded governance

## Human release — recovered 2026-09-07

At `2026-09-07T19:09:03.887Z`, Levi answered **“Yes authorized”** to the
explicit staged-release question in Codex task
`01a07901-1834-7fc2-9575-07013b67ebba` (turn
`01a07d46-235f-74d1-b395-cb2b29504c08`). That task acknowledged the release,
then stopped on its usage limit before persisting it or implementing code.
The continuation task recovered the original message and verified the repository
at `a655fc5`; no BUILD 6 implementation was present locally or on fetched branches.

This release accepts the output contract, execution order and acceptance matrix
below for construction and disposable qualification. It resolves the earlier
qualification-order constraint: synthetic mechanism qualification may precede
actual credential binding. The concrete runtime/session mechanism is selected
for this bounded implementation. Exact live credential/scope binding still must
precede canonical bootstrap-instance freeze and activation. Device observations,
deployment qualification, exact P1 acceptance and human Metabolize remain pending.
The proposal wording below is retained as the exact package presented for release;
its statements that release is pending are superseded by this decision record.

The domain mini-Move is [Metabolized](../deployment-shapes/ecb-human-domain-metabolize.md). The next behavior is a real protected human entrance and a reconstructible, enforced M2 initialization followed by the first lawful succession. Domain setup alone provides none of those behaviors.

## Alignment and proposed release

Preserve H=Levi, the accepted remit, exact P0 SHA-256 `686148f540860aca57a43d8cdf02ee15a0f6314d14b54736e6baf6f1846a7664`, direct WebAuthn, session continuity and the accepted hostname. Use the [runtime boundary](008-build-6-runtime-boundary.md), with the transaction and exact-policy constraints in the [technical proposal](008-build-6-technical-shape.md) only where the later WebAuthn/session proposal retains them. Supabase Auth/email enrollment and repeated passkey approval per artifact are superseded.

Recommend releasing BUILD 6 as staged implementation: construction and disposable qualification first, qualified deployment and protected enrollment second, exact live binding and activation last. These are effect boundaries within BUILD 6, not new Build Units or permission to open BUILD 7. Freeze the canonical bootstrap instance only after its actual credential/scope binding; test identities must never stand for Levi or be copied into canonical authority.

This staging needs explicit disposition because earlier technical proposals held executable qualification behind actual identity/scope binding. Building the enrollment mechanism first avoids requiring an enrolled credential before the mechanism exists. The original human H/remit/P0 decisions survive unchanged. This document proposes that scheduling resolution; it does not silently declare the previous freeze condition satisfied.

## Output contract proposed for release

| Deliverable | Completion observation |
|---|---|
| Private governance migration and restricted roles | Accepted predecessor reconstructs; native records preserve Referent identity; ordinary callers cannot write decisions, policy pointers or history outside their allowed entries. No changes to closed kernel semantics. |
| Same-origin human service on the isolated Vercel project | Verified HTTPS, protected registration/authentication/session endpoints, exact decision view, own-pending-decision withdrawal and truthful recovery view. No canonical owner/service-role/JWT-signer secret in this deployment. |
| Human-operated setup and exact binding record | External setup basis, canonical scope, H/remit/P0, exact credential set and RP origin are inspectable and bound through the protected root route. Registration alone leaves keys inert. |
| Bounded executor adapter and signposting | Exact grant/subject/predecessor inputs; descriptions state caller permissions, effects, rejection and recovery. No human-decision privilege or human browser control available to the ordinary operating agent. |
| Qualification and activation receipts | Paired successful/adversarial checks, actual concurrency/recovery evidence, deployed credential inventory, human device observations and canonical M2 reconstruction. Unrun checks remain unrun. |

Use the proposed private subject/scope/credential/ceremony/session/decision/transition responsibilities; each physical table must justify its integrity or transaction need. Do not overload BUILD 5A standing Events or BUILD 5B Artifact roles with policy lifecycle semantics. No new universal Actor, status ladder, policy engine or additional database.

## Execution order and actual human participation

1. **Construct and qualify locally.** Implement the migration, service, enrollment helper and executor against the accepted predecessor. Use explicitly synthetic principals in disposable resources. They demonstrate mechanism behavior, not Levi's identity or a canonical genesis. Pin and inspect the actual installed WebAuthn package; prior source inspection is not executable qualification.
2. **Prepare qualified deployment.** Reconcile the migration and current canonical predecessor; provision only restricted service/executor credentials under installation custody. Deploy the real service in `ecb-human`. Verify HTTPS and the ordinary-agent tool/credential exclusion before presenting an enrollment link. Canonical governance remains inactive.
3. **Levi enrolls.** A human-operated installer opens a short-lived enrollment capability bound to the retained external basis and proposed exact scope. Generate/deliver that capability outside the ordinary operating agent; do not expose it in chat, URLs captured in agent context or logs. A first visitor or arbitrary registered key cannot claim H. Levi uses the native passkey ceremony at the accepted origin.
4. **Bind the actual instance once.** Show the resulting public credential set, the exact scope within canonical `ecb-v2-brain`, and the already accepted H/remit/P0. The human's setup completion binds the new credential/scope facts through the protected root route. It does not repeat their settled normative acceptance. Include a second authenticator if available; qualify which failure it covers. A synced passkey helps with device loss; only a separately held credential addresses loss of that credential ecosystem. Never label redundancy verified without observing it.
5. **Activate M2.** Retain the exact externally authorized initialization basis in a prior committed record. One serialized transaction activates exact P0, designates H/remit and exhausts bootstrap permission, retaining ordered history and current state together. Registration is not the initialization grant. Unknown outcome must be reconciled before another effect; exact committed success is recovered without another genesis or another approval.
6. **Exercise ordinary governance.** Present the exact first P1 under operative P0/H. Levi makes that new decision once through the protected session; it is not covered by earlier P0 acceptance. Execute its committed authorization and reconstruct the result. The explanation-toggle P1 remains a proposed specimen until its exact decision is made. Close BUILD 6 only after its evidence is reviewed against the released contract.

Ordinary inspection and execution/recovery of committed grants do not prompt again for authentication. Propose the existing configurable 24-hour inactivity/seven-day absolute session limits for initial qualification; polling cannot extend them. Measure actual interruptions. A fresh login establishes a session, not another policy grant. H's own unconsumed-decision withdrawal is the bounded intervention available here; it is not a global pause service.

## Acceptance and enforcement

All twelve inherited pressures remain APPLICABLE. These expectations are proposed for freeze with the implementation release; they are not executed PASS results or a silently installed TEST_AUTHORITY.

| Pressure | Required positive/negative discrimination | Mode / surface |
|---|---|---|
| duplicate/replay | Exact committed retry returns its result; replay cannot create another genesis, session from a spent assertion, or succession. | STRUCTURAL / transaction, controller |
| concurrency | Real competing genesis/succession and withdrawal/execution attempts serialize; winner commit and rollback both preserve one lawful outcome. | STRUCTURAL / scope lock and transaction |
| stale state/basis | Current-basis decision/effect succeeds; stale predecessor or changed required binding rejects. | AUTHORITY + STRUCTURAL / authorization check and transaction |
| partial failure/rollback | Failure before commit leaves no partial active governance; lost acknowledgement after commit recovers exact success. | STRUCTURAL / atomic transaction and recovery controller |
| restart/reconstruction | Cold service/client recovers exact subjects, prior basis, exhaustion and outcome from durable records. Missing or inconsistent state cannot license a retry. | STRUCTURAL + OBSERVATIONAL / reconstruction and checked receipt |
| wrong identity/role | Enrolled/bound human succeeds; arbitrary registration, swapped key/user handle, missing UV, cross-origin assertion and operating-agent decision issuance reject. | AUTHORITY + STRUCTURAL / verifier and grants |
| wrong version | Exact permitted policy succeeds; altered bytes/digest, duplicate JSON keys, unknown fields, wrong types and cross-scope references reject. | STRUCTURAL / strict decoder and transaction |
| basis drift/revocation | Eligible unwithdrawn decision succeeds; own withdrawal or invalid session prevents the affected new admission. Past valid grants are not silently revoked by logout. | AUTHORITY + STRUCTURAL / session checks and serialized decision history |
| checker bypass/false PASS | Same checking route accepts the valid case and rejects its violating control. Direct function/table access cannot bypass the effect check; generated success prose has no force. | STRUCTURAL + OBSERVATIONAL / grants, tests and receipt |
| retry/idempotency | Same request/input recovers; same request with different input conflicts. Scope-locked absence is distinguished from unobserved outcome. | STRUCTURAL / transaction and recovery controller |
| time/order ambiguity | Prior top-level commitment is required; same-transaction/savepoint acceptance cannot authorize execution. Timestamps do not determine authority order. | STRUCTURAL / commitment checks and causal references |
| unauthorized mutation | Ordinary role cannot enlarge remit, replace H, reopen bootstrap, change history or issue a human decision. CSRF and session-forgery controls fail as intended. | AUTHORITY + STRUCTURAL / controller, privileges and transaction |

Additional observations: actual iPhone/Mac ceremony; no duplicate confirmation for one decision; execution continues under its valid grant without a new login; alternate-client withdrawal works without the dashboard; expired sessions and login-response loss recover without manufacturing a grant. Synthetic WebAuthn tests do not prove physical-device behavior. Verify fixed function search paths, ownership/default grants, role inheritance and operating-agent browser access in the actual deployment.

## Stop, repair and recovery

Before live binding, a failed rehearsal or deployment can be repaired without authority activation. Do not remove unrelated predecessor data or conceal failed attempts. Before canonical migration, retain the exact migration and predecessor/recovery evidence; a mismatch stops that installation for reconciliation.

After a committed initialization, rollback is not a bootstrap reset. Recover the immutable history and exact result. A bad future policy uses the lawful successor route within the supported language; it cannot be repaired by deleting history. Corruption, all-credential loss or a need to replace H is a separately authorized recovery problem, not an invented ordinary permission.

Stop the affected seam if ordinary agents possess human-session control or verifier/owner credentials, if the proposed transaction cannot preserve M2, if source reconciliation exposes a frozen-invariant conflict, or if live binding cannot be attributable to the protected external root. Continue independent local repair where its scope remains authorized.

## Recommendation, remaining input and limits

Approve the staged release above so enrollment software can be built and tested before real credentials are bound, while retaining the exact live-instance freeze and activation conditions. The input changes implementation permission and qualification ordering; it does not change H, remit, P0 or the hostname.

Reconsider this recommendation if isolated qualification cannot be kept separate from live authority, the hosted verifier cannot exclude ordinary-agent control, or initialization gains external side effects that cannot share the canonical transaction. Those findings require a changed mechanism or scoped authorization, not more frequent approval buttons.

No BUILD 6 code, tests, credentials, canonical migration, service deployment or governance activation was performed by preparing this package. No source qualification or Shape closure is claimed from documentary completeness. BUILD 7+, broad action envelopes, global slowdown/pause, final dashboards and total-loss credential recovery remain outside this release.
