STATUS: BUILD 8 SHAPE CLOSED / PRE-MOVE METABOLIZE COMPLETE
DISPOSITION: HUMAN ACCEPTED / REPOSITORY NORMALIZATION CANDIDATE
ROLE: BUILD 8 Shape closure + cold Move reentry record
DATE: 2026-09-11 America/Phoenix

# BUILD 8 — Action Envelope Lifecycle

## Human closure

BUILD 8 Sense and Shape are closed by explicit human authority in Linear.

Controlling coordination records:

- ECO-97 — accepted BUILD 8 Sense closure and patches.
- ECO-98 — human Shape closure, comment `4872ef52-488c-41bb-be4c-a99da401e27e`.
- Astra Shape candidate — Linear document `a2b6c22e-9690-439b-8dcc-8f7179f6349c`.
- Integrated review constraints I1–I9 — ECO-98 comment `6165616d-f60e-4726-889e-efe356b3868a`.
- External-review amendments E1–E6 — ECO-98 comment `608c8937-40a6-4470-b1b7-d0f319e998d9`.
- Ratified structural-leverage law — ECO-98 comment `309764b9-69e7-473b-b8b2-d2926ef9e9b2`.
- Pre-Move seam Metabolize — ECO-102.

Linear/human records remain the authority for the human phase transition. This repository record is the normalized cold-reentry projection of that authority; it does not create Move authority by itself.

## Accepted bounded mechanism

The accepted Shape is:

`exact envelope`
→ `finite bounded qualification`
→ `protected authorization + separately inspectable designation`
→ `condition-specific admission/effect checks`
→ `committed execution-start`
→ `atomic synthetic native effect + terminal lifecycle record with independent semantics`
→ `read-only replay/recovery`
→ `explicit serialized reconcile/fence before any later effectful attempt`

R1–R6 are behavioral responsibilities and acceptance axes, not six mandated physical components.

The proving specimen remains one state-conditioned non-idempotent logical action on a disposable synthetic target with a maximum permitted effect of one occurrence.

## Accepted review constraints

The accepted Shape includes I1–I9 and E1–E6. The most load-bearing normalized consequences are:

1. `(scope S, action A)` is the exact episode/recovery locator. No extra BUILD 8 episode root is earned.
2. Target-native revision increments only for committed native target changes; non-target dependencies carry their own exact tokens.
3. Synthetic logical tick is fixture-local, strictly increasing, and never substituted by wall/database time.
4. Effect, evidence-about-effect, acknowledgement, and legitimacy remain independently recoverable even if the native effect and terminal lifecycle record commit atomically in one transaction.
5. `b8_event` may share the physical Artifact store while retaining logical Event semantics; shared storage does not collapse semantics.
6. Actor identity derives from protected authentication context, not producer payload.
7. The legal predecessor/head is reconstructed from complete bounded history under the scoped lock. No newest-record repair and no separate BUILD 8 currentness registry.
8. Every consequential requirement must name an actual enforcement surface.
9. The four local physical routing roles are retained only where they mechanically remove deterministic liability from discretionary interpretation: `b8_basis`, `b8_envelope`, `b8_evaluation`, `b8_event`. No qualification-start Event is earned.
10. Reuse of `public.artifacts` requires role-partitioned preparation triggers. The BUILD 5B checker function text must remain unchanged; historical BUILD 5B checker bindings must survive the BUILD 8 migration. 5B, 7, and 8 role families must each hit exactly one preparation trigger.
11. P12 UNKNOWN/IN_FLIGHT must arise from a real observation/serialization condition such as bounded failure to acquire the shared scope lock while an unresolved attempt may still be executing. A doctored/disconnected reader is not acceptance evidence.
12. Exact replay is observational and never fences. Explicit reconcile is the operation that may append a terminal no-effect fence after complete serialized inspection.
13. BUILD 8 uses separate bounded custody and must not gain mutation authority over BUILD 7 `current_event` merely by sharing the lock row.
14. Effectful controller entry requires READ COMMITTED and fails closed under another isolation level. The acceptance harness must prove the blocked-waiter case under the actual accepted level.
15. Closure claims are limited to the atomic-ledger target class: native effect and native target-operation record commit atomically inside the accepted PostgreSQL transaction boundary. No general exactly-once or arbitrary external-effect guarantee travels.
16. No separate aggregate closure digest is earned. The immutable envelope Artifact digest commits to the exact manifest of individually addressed revision-pinned dependencies.
17. Authorization and designation may co-instantiate in one protected act because the accepted requirement is semantic distinctness and independent inspectability, not necessarily distinct acts.

## Ratified structural-leverage law

The following is governing:

> **Physicalize deterministic liability; preserve semantic freedom where interpretation remains valuable.**

Operational boundary:

> **Structure what must not depend on judgment; preserve judgment where structure would destroy information.**

This law complements `Structure is earned`. It prevents under-structuring without giving schema or generic machinery an automatic presumption. Moving a stable mechanically decidable consequential distinction into generic payload/model interpretation does not count as simplification merely because visible schema vocabulary shrinks.

The law is normalized into `docs/invariants.md` and `docs/build-contract.md`. It creates no BUILD 8 runtime component by itself.

## Pre-Move Metabolize

### Expected

Shape was expected to derive the smallest concrete R1–R6 mechanism, preserve the Sense distinctions, avoid later-BUILD machinery, provide a decision-complete Move contract, and leave implementation unopened.

### Observed

The candidate met that objective but review exposed several places where apparent minimality had hidden deterministic liability:

- generic Artifact reuse collided with the inherited BUILD 5B trigger/checker binding;
- synthetic UNKNOWN needed a real observation-unavailability condition rather than a test wrapper;
- replay-triggered recovery could become a hidden mutating/fencing path and starve progress;
- reuse of BUILD 7 custody would over-grant authority at the Master-Key pointer seam;
- row-lock correctness depended on transaction isolation and therefore had to become an explicit controller precondition;
- proof wording needed to prevent local atomic-ledger closure from traveling as a general exactly-once claim.

These findings did not overturn the core mechanism. They sharpened where Control must be physical and where Awareness must withhold inference.

### FACT

- BUILD 8 Sense and Shape are human-closed.
- Move remains unopened and unimplemented.
- The accepted Shape includes I1–I9 and E1–E6.
- The structural-leverage law is human-ratified and governing.
- Canonical ECB currently does not contain a BUILD 7 installation.
- The accepted Move Output Contract remains a proposed proof/implementation obligation set; no BUILD 8 PASS has been executed.

### INFERENCE

- The external-review defects are local specification/enforcement-placement corrections, not evidence that the accepted R1–R6 decomposition is wrong.
- A cold Move worker should now be able to implement without making architecture choices if it uses this record plus the referenced Linear records and governing repository sources.
- The most important new architectural lesson is that minimality must measure where consequential liability lives, not merely visible object/table count.

### UNKNOWN / Question Forward

These remain outside the bounded Move unless triggered by implementation evidence:

- real Master-Key designation authority/currentness;
- real action authority and relation to BUILD 6 remit;
- canonical BUILD 7 installation for real reliance;
- targets whose effects escape the accepted atomic-ledger transaction boundary;
- broader semantic-method generalization;
- generalized executor/runtime/deployment architecture;
- BUILD 9 recursion;
- BUILD 10 propagation;
- final UX;
- AP-10 formalization absent a concrete lossy-abstraction trigger.

## FCA reflection

**Freedom:** the Astra worker and external reviewer were allowed to search broadly, challenge the candidate, and independently rediscover implementation hazards without being constrained to historical mechanism imitation.

**Control:** promotion was concentrated at consequence seams: human Shape closure, exact accepted patches, protected authority/custody boundaries, transaction isolation, trigger routing, and the future Move commission. Exploration itself was not over-gated.

**Awareness:** review caught basis/implementation mismatches that were invisible at the prose-only Shape level: the 5B checker-binding collision, fake UNKNOWN risk, replay starvation, custody overreach, isolation dependence, and proof-travel risk. Those observations returned through Control and changed the accepted Shape before Move.

**Dynamic balance:** the seam was initially under-regulated at deterministic implementation boundaries and correctly overhauled without converting the entire architecture into hard-coded machinery. No evidence supports a new universal runtime or policy layer.

## Move reentry boundary

Move is **not opened by this record**.

A future BUILD 8 Move commission must:

- treat this file as the repository-normalized Shape closure/reentry record;
- live-read the controlling Linear closure and patch records when available;
- implement only the accepted bounded mechanism;
- execute the accepted P01–P14 proof surface as amended by E1–E6;
- preserve historical BUILD 5B checker binding while partitioning the shared Artifact trigger surface;
- fail closed on any implementation fact that contradicts the accepted mechanism rather than silently redesigning Shape;
- return to Shape/human adjudication if a named falsifier or new architecture choice is required.

## State at Metabolize close

```text
BUILD_8_SENSE=CLOSED
BUILD_8_SHAPE=CLOSED; HUMAN_ACCEPTED; I1_I9_E1_E6_CONTROLLING
BUILD_8_PRE_MOVE_METABOLIZE=COMPLETE
BUILD_8_MOVE=UNOPENED
BUILD_8_IMPLEMENTATION=NONE
STRUCTURAL_LEVERAGE_LAW=RATIFIED; GOVERNING; REPOSITORY_NORMALIZED
REAL_MASTER_KEY_AUTHORITY=QUESTION_FORWARD
REAL_ACTION_AUTHORITY=QUESTION_FORWARD
CANONICAL_BUILD7_DATABASE_INSTALL=NONE
BUILD_9_10=UNOPENED
```
